// PPT export stub — install pptxgenjs (npm install) to enable full export
import { toast } from "sonner";

export async function exportAllDashboardsToPPT(): Promise<void> {
  toast.error("PPT export unavailable — run npm install in the project to enable it.");
}
